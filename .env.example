DATABASE_URL="file:./dev.db"
# For production (e.g. Vercel + Neon): use a PostgreSQL URL
# DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"

# Admin password for /admin (staff, applications). If unset, admin is open (dev only).
ADMIN_PASSWORD="your-secret-admin-password"

# Optional: secret to sign staff session cookie (defaults to ADMIN_PASSWORD if unset)
# STAFF_SESSION_SECRET="your-staff-session-secret"
